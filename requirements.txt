# Core ML libraries (versions from setup.sh for RTX 3090 compatibility)
torch==2.0.1
torchvision==0.15.2
torchaudio==2.0.2
diffusers==0.18.2
transformers==4.30.2
accelerate==0.20.3
datasets==2.13.1
huggingface_hub==0.16.4

# Image processing (compatible versions)
Pillow>=8.0.0
opencv-python-headless>=4.5.0,<4.9.0

# Scientific computing (NumPy compatible with PyTorch 2.0.1)
numpy>=1.21.0,<2.0
scipy>=1.9.0
scikit-learn>=1.1.0

# Visualization (compatible versions)
matplotlib>=3.5.0
seaborn>=0.11.0
plotly>=5.10.0
scikit-image>=0.19.0

# Experiment tracking (compatible versions)
wandb>=0.12.0
tensorboard>=2.8.0

# Utilities (compatible versions)
tqdm>=4.60.0
omegaconf>=2.1.0
safetensors==0.3.1
einops>=0.6.0
timm>=0.6.0
requests>=2.25.0
packaging>=20.0
regex>=2022.1.18
importlib-metadata>=4.0.0

# COCO dataset support
pycocotools>=2.0.4

# Web interfaces (compatible versions)
gradio==4.8.0
streamlit>=1.20.0
fastapi==0.104.1
pydantic==2.4.2

# Development tools (optional)
pytest>=6.0.0
black>=22.0.0
isort>=5.10.0
flake8>=4.0.0

# Jupyter (optional)
jupyter>=1.0.0
notebook>=6.4.0

# Installation notes:
# Use setup.sh for RTX 3090 compatibility
# These versions are tested to work together without conflicts
# PyTorch index URL: https://download.pytorch.org/whl/cu117
